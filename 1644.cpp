// https://cses.fi/problemset/task/1644

#include <bits/stdc++.h>

#define ll long long 

using namespace std;

int main()
{
    ll n, a, b;
    cin >> n >> a >> b;

    vector<ll> x(n);

    for (int i = 0; i < n; i++)
        cin >> x[i];

    // for size between a and b 
}